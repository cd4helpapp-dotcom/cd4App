import ManagedContentScreen from '../ui/settings/ManagedContentScreen';
export default function PublicTermsPage() {
  return <ManagedContentScreen contentKey="terms" fallbackTitle="Terms of Service" />;
}
