import React from 'react';
import ManagedContentScreen from '../../ui/settings/ManagedContentScreen';

export default function TermsPage() {
  return <ManagedContentScreen contentKey="terms" fallbackTitle="Terms & Service" />;
}

